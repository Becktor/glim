session_name: glim_ros
environment:
  ROS_DOMAIN_ID: "0"  # Adjust as needed
  ROS_USE_SIM_TIME: "true"
shell_command_before:
  - source ~/ros2_ws/install/setup.bash
windows:
  - window_name: glim_ros_workspace
    layout: tiled
    panes:
      - shell_command:
          - ros2 run glim_ros glim_rosnode --ros-args -p config_path:=/glim/config
      - shell_command:
          #- ros2 bag play /glim/data/jsu-short-rm --clock
          - ros2 bag play /glim/data/ox-stair --clock
      - shell_command:
          - rviz2 -d /root/ros2_ws/src/glim/config/rviz.rviz
      - shell_command:
          - echo "This is an extra pane for commands."